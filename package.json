{
  "name": "txukun",
  "version": "1.0.0",
  "private": true,
  "description": "Euskarazko testu zuzentzailea — Basque text correction tool",
  "homepage": "https://itzune.eus/txukun/",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.6.0"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^5.0.0"
  }
}
